apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  type: NodePort
  ports:
    - port: 8888
      nodePort: 31000
      targetPort: 8888
      protocol: UDP
  #  externalIPs:
  #    - 42.192.43.222
  selector:
    app: auth
#  sessionAffinity: ClientIP
#  sessionAffinityConfig:
#    clientIP:
#      timeoutSeconds: 3600