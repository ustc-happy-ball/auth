apiVersion: v1
kind: Service
metadata:
  name: auth-srv
spec:
  type: NodePort
  ports:
    - port: 8889
      nodePort: 32000
      targetPort: 8889
      protocol: UDP
  #  externalIPs:
  #    - 42.192.43.222
  selector:
    app: auth
#  sessionAffinity: ClientIP
#  sessionAffinityConfig:
#    clientIP:
#      timeoutSeconds: 3600